本来在不添加新的功能的前提下，希望通过静态网站实现全部功能
但是在图片展示页面，我不想手动输入全部url，于是我想只输入文件名，让前端自己遍历。但是查了一下，前端/浏览器是无法直接访问本地文件系统的(为了保证安全)，需要后端程序来读取。
解决的方法暂时没有，后面可能考虑手动脚本输入url，暂时不打算涉猎后端
现在做出来一个文件选择器<input type="file" id="folderInput" webkitdirectory directory multiple>
可以手动选择文件导入里面的图片，但是无法默认，不是个好选择
